# 🎯 Heroes Platform - Основные правила

## 📁 Структура проекта

**Основная рабочая папка:** `heroes-platform/`

### Ключевые компоненты:
- **MCP Server:** `heroes-platform/mcp_server/` - основной MCP сервер
- **Telegram MCP:** `heroes-platform/telegram-mcp/` - интеграция с Telegram
- **Playwright MCP:** `heroes-platform/playwright-mcp/` - браузерная автоматизация
- **Ghost CMS:** `heroes-platform/rickai-mkdocs/` - документация и публикации
- **Code Quality:** `heroes-platform/code-quality-template/` - шаблоны качества кода

## 🔧 Конфигурация

### Основные файлы:
- **pyproject.toml** - основной файл конфигурации проекта
- **setup.py** - скрипт установки зависимостей
- **Makefile** - команды для управления проектом
- **run_tests.py** - альтернативный тестовый раннер

### CI/CD:
- **.github/workflows/ci.yml** - основной CI/CD pipeline
- **.github/workflows/ghost-publication-tests.yml** - тесты для Ghost CMS

## 🧪 Тестирование

### Структура тестов:
- **tests/unit/** - модульные тесты
- **tests/integration/** - интеграционные тесты
- **tests/e2e/** - end-to-end тесты
- **tests/manual/** - ручные тесты
- **tests/playwright/** - браузерные тесты

### Команды тестирования:
```bash
# Запуск всех тестов
cd heroes-platform
python run_tests.py

# Запуск через pytest
python -m pytest tests/ -v

# Запуск через Makefile
make test
```

## 🚀 Разработка

### Установка и настройка:
```bash
cd heroes-platform
python setup.py
make install-dev
```

### Основные команды:
```bash
make lint          # Проверка кода
make format        # Форматирование кода
make test          # Запуск тестов
make run-server    # Запуск MCP сервера
```

## 📋 Правила работы

### 1. Структура файлов:
- Все основные файлы должны быть в `heroes-platform/`
- Подпроекты в отдельных папках
- Общие зависимости в основном `pyproject.toml`

### 2. Тестирование:
- Все изменения должны покрываться тестами
- Использовать `run_tests.py` для обхода проблем с pytest
- Поддерживать unit, integration и e2e тесты

### 3. Качество кода:
- Использовать pre-commit hooks
- Следовать стандартам black, ruff, mypy
- Проверять безопасность через bandit и safety

### 4. Документация:
- Использовать символические ссылки для mkdocs
- НЕ копировать файлы, только ссылки
- Следовать правилам из `mkdocs_symlinks.mdc`

## 🔍 Диагностика проблем

### Частые проблемы:
1. **Модуль platform не найден** - использовать `run_tests.py`
2. **Пути к файлам** - все пути относительно `heroes-platform/`
3. **MCP сервер не запускается** - проверить PYTHONPATH в `.cursor/mcp.json`

### Отладка:
```bash
# Проверка структуры
ls -la heroes-platform/

# Проверка зависимостей
cd heroes-platform
python -c "import sys; print(sys.path)"

# Проверка MCP сервера
cd heroes-platform/mcp_server
python run_server.py --help
```

## 🎯 JTBD

**Когда работаете с heroes-platform:**
1. Всегда начинайте с `cd heroes-platform/`
2. Используйте `pyproject.toml` как основной источник зависимостей
3. Запускайте тесты через `run_tests.py` или `make test`
4. Следуйте структуре проекта и не создавайте дублирующихся файлов
5. Используйте символические ссылки для документации
description:
globs:
alwaysApply: true
---
