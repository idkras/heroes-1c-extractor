name: Linter CI/CD

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  lint-and-fix:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install ruff
        pip install -r requirements.txt
        
    - name: Run linter with auto-fix
      run: |
        echo "🔧 Автоматическое исправление ошибок линтера..."
        python scripts/fix_linter_errors.py
        
    - name: Check for remaining linter errors
      run: |
        echo "🔍 Проверка оставшихся ошибок линтера..."
        ruff check src/ tests/ scripts/ || echo "⚠️ Найдены ошибки, требующие ручного исправления"
        
    - name: Format code
      run: |
        echo "🎨 Форматирование кода..."
        ruff format src/ tests/ scripts/
        
    - name: Commit fixes
      if: github.event_name == 'push'
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        git add .
        git diff --staged --quiet || git commit -m "🔧 Auto-fix linter errors [skip ci]"
        git push || echo "⚠️ Не удалось запушить исправления"

  test:
    runs-on: ubuntu-latest
    needs: lint-and-fix
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        
    - name: Run tests
      run: |
        echo "🧪 Запуск тестов..."
        python -m pytest tests/ -v --tb=short
        
    - name: Run linter check
      run: |
        echo "🔍 Финальная проверка линтера..."
        ruff check src/ tests/ scripts/
        
    - name: Check code formatting
      run: |
        echo "🎨 Проверка форматирования..."
        ruff format --check src/ tests/ scripts/
