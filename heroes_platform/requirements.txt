# Core dependencies (for compatibility with pyproject.toml)
# This file is kept for backward compatibility
# Main dependencies are managed in pyproject.toml

# Core dependencies
requests>=2.32.3
pytest>=8.3.4
pytest-asyncio>=0.24.0
numpy>=2.1.3
pydantic>=2.10.3
python-dotenv>=1.0.0

# Web scraping and parsing
selenium>=4.28.0
beautifulsoup4>=4.12.3
aiohttp>=3.11.11

# GraphQL and API
gql>=3.7.0
gql[requests]>=3.7.0

# PDF processing
PyPDF2>=3.0.1
weasyprint>=62.3

# Date and time utilities
arrow>=1.3.0

# JSON utilities
ujson>=5.10.0

# Ghost CMS integration
PyJWT>=2.10.1
markdown>=3.7.0

# Node.js integration for Lexical conversion
subprocess32>=3.5.4

# MCP and workflow
mcp[cli]>=1.13.0
playwright>=1.49.1
telethon>=1.41.2
nest-asyncio>=1.6.0

# Database
psycopg2-binary>=2.9.10
duckdb>=1.4.0

# Jupyter and Data Science
jupyterlab>=4.4.0
notebook>=7.0.0
ipykernel>=6.30.0
pandas>=2.3.0
pyarrow>=21.0.0

# Note: For new installations, use:
# cd heroes-platform
# python -m pip install -e ".[dev]"
#
# ⚠️ ВАЖНО: Все MCP серверы используют .venv Python для консистентности
# Создайте виртуальное окружение: python3.12 -m venv .venv
# Активируйте его: source .venv/bin/activate
