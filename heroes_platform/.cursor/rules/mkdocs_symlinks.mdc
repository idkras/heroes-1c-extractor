# 📋 Правила работы с документами в mkdocs

## ❌ ЗАПРЕЩЕНО копировать файлы

**НЕ используйте:**
- `cp` команды
- `copy` операции
- Создание дубликатов файлов
- Перемещение файлов из исходных папок

## ✅ ОБЯЗАТЕЛЬНО создавать ссылки

**ВСЕГДА используйте:**
- `ln -s` для создания символических ссылок
- Относительные пути к исходным файлам
- Сохранение исходных файлов на своих местах

## 🔗 Правильный процесс:

1. **Исходный файл** остается в своей папке (например, `[rick.ai]/knowledge base/...`)
2. **Ссылка** создается в mkdocs папке для документации
3. **Изменения** делаются только в исходном файле
4. **Синхронизация** автоматическая через ссылку

## 📁 Структура ссылок:

```bash
# Пример создания ссылки
ln -s "../../../../[rick.ai]/knowledge base/in progress/1. when new lead come/when mobile · appmetric · ajust/vipavenue.ru/vipavenue.adjust_appmetrica.md" vipavenue.adjust_appmetrica.md
```

## 🚨 Проверка:

**Перед созданием файла в mkdocs:**
1. Проверить что это ссылка: `ls -la filename.md`
2. Должно быть: `lrwxr-xr-x ... filename.md -> ../../../../исходный/путь/...`
3. НЕ должно быть: `-rw-r--r-- ... filename.md` (обычный файл)

## 🔧 Автоматизация:

**Используйте скрипт:**
```bash
cd heroes-platform/rickai-mkdocs
python create_symlinks.py
```

## 📋 Примеры правильных операций:

```python
# ✅ Правильно - создание ссылки
import os
os.symlink("../../../../исходный/путь/file.md", "target.md")

# ❌ Неправильно - копирование
import shutil
shutil.copy("source.md", "target.md")  # ЗАПРЕЩЕНО!
```

## 🎯 JTBD:

**Когда нужно добавить документ в mkdocs:**
1. Определить исходный файл
2. Создать символическую ссылку
3. Проверить что ссылка работает
4. НЕ копировать содержимое

## 🔍 Диагностика проблем:

**Если файл не отображается в mkdocs:**
1. Проверить что ссылка существует: `ls -la`
2. Проверить что исходный файл существует
3. Пересоздать ссылку если нужно

**Если изменения не синхронизируются:**
1. Убедиться что редактируете исходный файл
2. Проверить что ссылка указывает на правильный файл
3. Перезапустить mkdocs сервер
description:
globs:
alwaysApply: true
---
